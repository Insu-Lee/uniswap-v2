{
  "name": "inbrew",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat --network kairos run scripts/deploy.ts",
    "setting": "npx hardhat --network kairos run scripts/setting.ts",
    "swap": "npx hardhat --network kairos run scripts/swap.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.19"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0",
    "dotenv": "^16.4.7",
    "ethers": "^6.13.5"
  }
}
